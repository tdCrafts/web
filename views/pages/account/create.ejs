<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../../partials/head") %>

    <title>Create Account - crafty.tools</title>
    <meta name="description" content="Easily calculate wax and fragrance weights for multiple containers, waxes, and fragrances! Save your calculations and batches for easy management of your supplies.">
</head>
<body>
    <%- include("../../partials/header", {page:"account/create"}) %>
    <main>
        <div class="wrapper xs-wrapper">
            <section>
                <h2><i class="fa-solid fa-user-plus"></i> Create an Account</h2>
                <% if (messages.error) { %>
                <div class="alert alert-danger">
                    <strong>Validation Error</strong>
                    <br>
                    <%- messages.error %>
                </div>
                <% } %>
                <p>
                    Login with your email and password for extra calculator features, batch tracking, and more!
                </p>
                <form action="/account/create" method="post">
                    <div class="container container-sm">
                        <div class="form-group col-6">
                            <label for="firstName">First Name</label>
                            <input type="text" name="firstName" id="firstName" placeholder="Jane" required="required"<% if (messages.firstName) { %> value="<%= messages.firstName %>"<% } %>>
                        </div>
                        <div class="form-group col-6">
                            <label for="lastName">Last Name</label>
                            <input type="text" name="lastName" id="lastName" placeholder="Doe" required="required"<% if (messages.lastName) { %> value="<%= messages.lastName %>"<% } %>>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" name="email" id="email" placeholder="maker@gmail.com" required="required"<% if (messages.email) { %> value="<%= messages.email %>"<% } %>>
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" name="password" id="password" placeholder="Password" minlength="7" required="required">
                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">Confirm Password</label>
                        <input type="password" name="confirmPassword" id="confirmPassword" placeholder="Confirm Password" minlength="7" required="required">
                    </div>
                    <input type="submit" value="Sign Up" class="button">
                </form>
                <p>
                    Already have an account?
                    <a href="/account/login">Login</a>
                </p>
            </section>
        </div>
    </main>
    <%- include("../../partials/footer") %>
</body>
</html>
