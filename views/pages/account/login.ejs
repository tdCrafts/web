<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../../partials/head") %>

    <title>Login - crafty.tools</title>
    <meta name="description" content="Easily calculate wax and fragrance weights for multiple containers, waxes, and fragrances! Save your calculations and batches for easy management of your supplies.">
</head>
<body>
    <%- include("../../partials/header", {page:"account/login"}) %>
    <main>
        <div class="wrapper xs-wrapper">
            <section>
                <h2><i class="fa-solid fa-right-to-bracket"></i> Login to crafty.tools</h2>
                <% if (messages.info) { %>
                <div class="alert alert-info">
                    <strong>Information</strong>
                    <br>
                    <%- messages.info %>
                </div>
                <% } %>
                <% if (messages.error) { %>
                <div class="alert alert-danger">
                    <strong>Error</strong>
                    <br>
                    <%- messages.error %>
                </div>
                <% } %>
                <p>
                    Login with your email and password for extra calculator features, batch tracking, and more!
                </p>
                <form action="/account/login" method="post">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" name="email" id="email" placeholder="maker@gmail.com" required="required">
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" name="password" id="password" placeholder="Password" required="required">
                    </div>
                    <input type="submit" value="Login" class="button">
                </form>
                <p>
                    Don't have an account yet?
                    <a href="/account/create">Create an Account</a>
                </p>
            </section>
        </div>
    </main>
    <%- include("../../partials/footer") %>
</body>
</html>
